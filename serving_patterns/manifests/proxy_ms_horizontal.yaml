apiVersion: apps/v1
kind: Deployment
metadata:
  name: api-iris-proxy
  labels:
    app: api-iris-proxy
  namespace: serving-pattern
spec:
  selector:
    matchLabels:
      app: api-iris-proxy
  replicas: 3
  template:
    metadata:
      labels:
        app: api-iris-proxy
      name: api-iris-proxy
    spec:
      containers:
      - name: api-iris-proxy
        image: gcr.io/newagent-16e99/github.com/shibuiwilliam/ml-system-in-action/serving_patterns_proxy:latest
        command: ["./run_proxy.sh"]
        env:
        - name: PLATFORM
          value: kubernetes
        - name: CACHE_HOST
          value: redis.serving-pattern.svc.cluster.local
        - name: GUNICORN_UVICORN
          value: GUNICORN
        - name: WORKERS
          value: "2"
        - name: BACKLOG
          value: "128"
        - name: LIMIT_MAX_REQUESTS
          value: "2048"
        - name: MAX_REQUESTS_JITTER
          value: "256"
        - name: APP_NAME
          value: "src.api_composition_proxy.apps.proxy:app"
        - name: PORT
          value: "8000"
        - name: PROFILE
          value: "0"
        - name: QUEUE_NAME
          value: resnet50_api_queue
        - name: SERVICE_MOBILENETV2_TFS
          value: api-mobilenetv2.serving-pattern.svc.cluster.local:8893
        - name: SERVICE_RESNET50_ONNX
          value: api-resnet50-onnx.serving-pattern.svc.cluster.local:8891
        resources:
          limits:
            cpu: "2"
            memory: "1000Mi"
          requests:
            cpu: "2"
            memory: "1000Mi"
        ports:
        - containerPort: 8000
        volumeMounts:
          - name: pvc-nfs
            mountPath: "/mnt"
      volumes:
      - name: pvc-nfs
        persistentVolumeClaim:
          claimName: nfs

---

apiVersion: v1
kind: Service
metadata:
  name: api-iris-proxy
  labels:
    app: api-iris-proxy
  namespace: serving-pattern
spec:
  ports:
  - port: 8000
    protocol: TCP
  selector:
    app: api-iris-proxy
